# Sunseo (개인 할 일 관리) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 한 줄로 던져 넣고, 중요도 3단계(오늘/이번주/언젠가)로 나뉜 전체 목록을 한 화면에서 보는 개인용 할 일 관리 웹앱을 만든다.

**Architecture:** Next.js App Router의 서버 컴포넌트가 초기 목록을 읽고, 이후 모든 변경은 클라이언트가 낙관적으로 화면에 반영한 뒤 Server Action이 Supabase에 저장한다. 실패하면 이전 상태로 롤백하고 토스트를 띄운다. 정렬은 분수 순위(fractional index)로 계산하며, 이 계산은 DB도 React도 모르는 순수 함수로 분리해 단위 테스트한다.

**Tech Stack:** Next.js 16.2.12 (App Router, src 디렉터리), React 19.2.4, TypeScript 5, Tailwind CSS 4, Supabase (Postgres + Auth + RLS), `@supabase/ssr`, dnd-kit, Vitest, Playwright.

**Spec:** `docs/superpowers/specs/2026-07-31-personal-task-manager-design.md`

## Global Constraints

이 절의 내용은 모든 Task의 요구사항에 암묵적으로 포함된다.

- **버전 고정:** `next@16.2.12`, `react@19.2.4`, `react-dom@19.2.4`, `eslint-config-next@16.2.12`. 기존 `dosemo` 프로젝트와 동일하게 맞춘다.
- **Next.js 16 규약:** 라우팅 전 훅 파일은 `middleware.ts`가 **아니라** `proxy.ts`이며, 내보내는 함수 이름은 `middleware`가 아니라 `proxy`다. 이것은 Next 16에서 바뀐 부분이므로 기억이 아니라 이 문서를 따른다.
- **환경변수 이름:** `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`. 다른 이름을 쓰지 않는다. `service_role` 키는 어떤 형태로도 `NEXT_PUBLIC_` 접두사가 붙은 변수에 넣지 않는다.
- **인증 확인:** 서버 코드에서 사용자를 확인할 때는 `supabase.auth.getClaims()`를 쓴다. `getSession()`은 쿠키를 재검증하지 않으므로 인가 판단에 쓰지 않는다.
- **RLS:** 모든 정책은 `TO authenticated`와 `(select auth.uid()) = user_id`를 함께 쓴다. `TO authenticated`만 쓰면 로그인한 아무나 남의 행을 읽는다. UPDATE 정책은 `USING`과 `WITH CHECK`를 **둘 다** 갖는다. `WITH CHECK`가 없으면 행의 `user_id`를 남에게 넘길 수 있다.
- **이름 규칙:** DB 컬럼은 snake_case, TypeScript는 camelCase. 변환은 `src/lib/tasks/queries.ts`의 `rowToTask` 한 곳에서만 일어난다.
- **상수:** `STALE_AFTER_DAYS = 3` (`src/lib/staleness.ts`), `MIN_RANK_GAP = 1e-6` (`src/lib/ranking.ts`).
- **전제 도구:** Node 20 이상, npm.
- **Supabase는 원격 프로젝트를 쓴다.** 이 기계에는 Docker가 설치돼 있지 않아 로컬 스택(`supabase start`)을 띄울 수 없다. 개발도 테스트도 아래 프로젝트를 직접 쓴다.
  - URL: `https://cwxbzvevvoxdarekeldb.supabase.co`
  - Publishable key: `sb_publishable_0NCtH9t7_tbtV27W685pxg_I0h1uwCy` (브라우저에 노출되도록 설계된 공개 키다. 실제 방어선은 RLS다.)
  - Supabase CLI는 인증돼 있지 않다. `supabase link` / `db push` / `db reset` / `db advisors`를 쓸 수 없다. **스키마 변경은 대시보드 SQL Editor에서 사람이 실행한다.** 저장소의 `supabase/migrations/*.sql`은 무엇을 실행했는지 남기는 기록이며, 자동 적용되지 않는다.
- **원격을 쓰는 데서 오는 제약:** 테스트가 실제 프로젝트의 데이터베이스를 건드린다. 데이터를 만드는 테스트는 반드시 스스로 정리해야 하고, 어떤 테스트도 자기가 만들지 않은 행을 지워서는 안 된다.
- **커밋:** 각 Task의 마지막 단계에서 커밋한다. Task 중간에 커밋하지 않는다.
- **작업 디렉터리:** 모든 명령은 `C:\Users\s2382\sunseo`에서 실행한다. 셸은 PowerShell이다.

## File Structure

| 경로 | 책임 |
|---|---|
| `proxy.ts` | Next.js 진입점. `lib/supabase/proxy`의 `updateSession` 호출만 한다. |
| `src/lib/supabase/client.ts` | 브라우저용 Supabase 클라이언트 |
| `src/lib/supabase/server.ts` | 서버 컴포넌트·Server Action용 클라이언트 |
| `src/lib/supabase/proxy.ts` | 요청마다 인증 쿠키를 갱신하는 `updateSession` |
| `src/lib/ranking.ts` | 분수 순위 계산. 순수 함수. 외부 의존 없음 |
| `src/lib/staleness.ts` | "오래됨" 판정. 순수 함수 |
| `src/lib/tasks/types.ts` | `Task`, `Bucket`, `ActionResult` 타입 |
| `src/lib/tasks/queries.ts` | 읽기 + `rowToTask` 변환 |
| `src/lib/tasks/actions.ts` | 쓰기 Server Actions |
| `src/app/page.tsx` | 서버 컴포넌트. 초기 목록을 읽어 `TaskBoard`에 넘긴다 |
| `src/app/login/page.tsx`, `src/app/login/actions.ts` | 로그인 폼과 액션 |
| `src/components/TaskBoard.tsx` | 클라이언트 상태 소유. 낙관적 업데이트와 롤백 |
| `src/components/QuickAdd.tsx` | 입력창. 제출 콜백만 받는다 |
| `src/components/TaskGroup.tsx`, `TaskItem.tsx` | 그룹·항목 렌더 |
| `src/components/CompletedSection.tsx` | 접히는 완료 영역 |
| `src/components/Sidebar.tsx` | 요약과 검색. 집계값을 props로 받는다 |
| `src/components/Toast.tsx` | 오류·실행취소 토스트 |
| `supabase/migrations/*.sql` | 스키마와 RLS |

핵심 규칙 하나: **컴포넌트는 Supabase를 직접 부르지 않는다.** 그래야 UI를 데이터베이스 없이 테스트할 수 있다.

---

### Task 1: 프로젝트 부트스트랩과 테스트 러너

빈 Next.js 앱이 뜨고 Vitest가 도는 상태를 만든다. 이 Task가 끝나면 `npm run dev`와 `npm test`가 둘 다 성공한다.

**Files:**
- Create: `package.json`, `tsconfig.json`, `next.config.ts`, `eslint.config.mjs`, `src/app/layout.tsx`, `src/app/page.tsx`, `src/app/globals.css` (create-next-app이 생성)
- Create: `vitest.config.ts`
- Create: `src/lib/smoke.test.ts`
- Modify: `package.json` (test 스크립트 추가)

**Interfaces:**
- Consumes: 없음 (첫 Task)
- Produces: `npm test` = `vitest run`, `npm run dev`. 경로 별칭 `@/*` → `src/*`.

- [ ] **Step 1: Next.js 앱 스캐폴딩**

`create-next-app`은 대상 폴더에 낯선 파일(`docs/`)이 있으면 거부한다. 임시 폴더에 만든 뒤 위로 올린다.

```powershell
npx --yes create-next-app@16.2.12 .tmp-scaffold --typescript --tailwind --eslint --app --src-dir --use-npm --disable-git --import-alias "@/*"
Get-ChildItem .tmp-scaffold -Force | ForEach-Object { Move-Item $_.FullName -Destination . -Force }
Remove-Item .tmp-scaffold -Recurse -Force
```

- [ ] **Step 2: 개발 서버가 뜨는지 확인**

Run: `npm run dev`
Expected: `Ready in ...` 출력 후 `http://localhost:3000`에서 Next.js 기본 페이지가 보인다. 확인했으면 Ctrl+C로 종료.

- [ ] **Step 3: Vitest 설치**

```powershell
npm install --save-dev vitest@^3
```

- [ ] **Step 4: Vitest 설정 파일 작성**

`vitest.config.ts`:

```typescript
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    environment: 'node',
    include: ['src/**/*.test.ts'],
  },
})
```

- [ ] **Step 5: test 스크립트 추가**

`package.json`의 `scripts`에 다음 줄을 추가한다:

```json
"test": "vitest run"
```

- [ ] **Step 6: 스모크 테스트 작성**

`src/lib/smoke.test.ts`:

```typescript
import { describe, expect, it } from 'vitest'

describe('test runner', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2)
  })
})
```

- [ ] **Step 7: 테스트 실행**

Run: `npm test`
Expected: PASS, `1 passed`

- [ ] **Step 8: .env.local.example 작성**

`.env.local.example`:

```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
```

`.gitignore`에 `.env.local`이 이미 포함되어 있는지 확인하고, 없으면 추가한다.

- [ ] **Step 9: 커밋**

```powershell
git add -A
git commit -m "chore: scaffold Next.js app with Vitest"
```

---

### Task 2: 분수 순위 계산 (`lib/ranking.ts`)

이 프로젝트에서 가장 미묘한 로직이다. DB도 React도 모르는 순수 함수로 두고 테스트를 여기에 집중한다.

**Files:**
- Create: `src/lib/ranking.ts`
- Test: `src/lib/ranking.test.ts`
- Delete: `src/lib/smoke.test.ts` (역할을 다했다)

**Interfaces:**
- Consumes: 없음
- Produces:
  - `MIN_RANK_GAP: number` (= `1e-6`)
  - `rankBetween(before: number | null, after: number | null): number`
  - `needsRebalance(before: number | null, after: number | null): boolean`
  - `rebalancedPositions(count: number): number[]`

`before`는 놓일 자리 **바로 위** 항목의 `position`, `after`는 **바로 아래** 항목의 `position`이다. 목록 맨 위면 `before`가 `null`, 맨 아래면 `after`가 `null`, 빈 목록이면 둘 다 `null`이다.

- [ ] **Step 1: 실패하는 테스트 작성**

`src/lib/ranking.test.ts`:

```typescript
import { describe, expect, it } from 'vitest'
import { MIN_RANK_GAP, needsRebalance, rankBetween, rebalancedPositions } from './ranking'

describe('rankBetween', () => {
  it('빈 목록에 넣으면 0이다', () => {
    expect(rankBetween(null, null)).toBe(0)
  })

  it('맨 위로 보내면 첫 항목보다 1 작다', () => {
    expect(rankBetween(null, 5)).toBe(4)
  })

  it('맨 아래로 보내면 마지막 항목보다 1 크다', () => {
    expect(rankBetween(5, null)).toBe(6)
  })

  it('두 항목 사이면 중간값이다', () => {
    expect(rankBetween(1, 2)).toBe(1.5)
  })

  it('음수 구간에서도 중간값이다', () => {
    expect(rankBetween(-4, -2)).toBe(-3)
  })

  it('반복 삽입해도 항상 두 값 사이에 있다', () => {
    let before = 0
    const after = 1
    for (let i = 0; i < 20; i++) {
      const next = rankBetween(before, after)
      expect(next).toBeGreaterThan(before)
      expect(next).toBeLessThan(after)
      before = next
    }
  })
})

describe('needsRebalance', () => {
  it('간격이 충분하면 false다', () => {
    expect(needsRebalance(1, 2)).toBe(false)
  })

  it('간격이 한계 미만이면 true다', () => {
    expect(needsRebalance(1, 1 + MIN_RANK_GAP / 2)).toBe(true)
  })

  it('한쪽 끝이면 재배열이 필요 없다', () => {
    expect(needsRebalance(null, 1)).toBe(false)
    expect(needsRebalance(1, null)).toBe(false)
    expect(needsRebalance(null, null)).toBe(false)
  })
})

describe('rebalancedPositions', () => {
  it('0부터 1씩 증가하는 값을 준다', () => {
    expect(rebalancedPositions(3)).toEqual([0, 1, 2])
  })

  it('빈 목록이면 빈 배열이다', () => {
    expect(rebalancedPositions(0)).toEqual([])
  })
})
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `npm test`
Expected: FAIL — `Failed to resolve import "./ranking"`

- [ ] **Step 3: 최소 구현 작성**

`src/lib/ranking.ts`:

```typescript
/**
 * 목록 순서를 분수 순위(fractional index)로 다룬다.
 * 항목 하나를 옮길 때 이웃 두 값 사이의 새 값만 계산하면 되므로,
 * 드래그 한 번에 목록 전체를 다시 번호 매기는 쓰기 폭발이 없다.
 */

/** 두 순위가 이보다 가까워지면 부동소수점 정밀도가 위태로워진다. */
export const MIN_RANK_GAP = 1e-6

/**
 * `before`(바로 위 항목)와 `after`(바로 아래 항목) 사이에 놓일 순위를 준다.
 * 목록 맨 위면 before가 null, 맨 아래면 after가 null, 빈 목록이면 둘 다 null이다.
 */
export function rankBetween(before: number | null, after: number | null): number {
  if (before === null && after === null) return 0
  if (before === null) return after! - 1
  if (after === null) return before + 1
  return (before + after) / 2
}

/** 이 자리에 더 끼워 넣기 전에 그룹을 재배열해야 하는지 판단한다. */
export function needsRebalance(before: number | null, after: number | null): boolean {
  if (before === null || after === null) return false
  return Math.abs(after - before) < MIN_RANK_GAP
}

/** 재배열할 때 쓸 순위 목록. 항목 순서는 그대로 두고 값만 1 간격으로 편다. */
export function rebalancedPositions(count: number): number[] {
  return Array.from({ length: count }, (_, i) => i)
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test`
Expected: PASS, ranking 테스트 11개 통과

- [ ] **Step 5: 스모크 테스트 삭제**

```powershell
Remove-Item src/lib/smoke.test.ts
npm test
```

Expected: PASS, ranking 테스트만 남는다

- [ ] **Step 6: 커밋**

```powershell
git add -A
git commit -m "feat: add fractional ranking calculation"
```

---

### Task 3: 오래됨 판정 (`lib/staleness.ts`)

`today`에 들어온 지 3일이 지났는데 아직 안 끝낸 항목을 표시하기 위한 판정이다. 자동으로 옮기지는 않는다.

**Files:**
- Create: `src/lib/tasks/types.ts`
- Create: `src/lib/staleness.ts`
- Test: `src/lib/staleness.test.ts`

**Interfaces:**
- Consumes: 없음
- Produces:
  - `type Bucket = 'today' | 'week' | 'someday'`
  - `type Task = { id: string; title: string; bucket: Bucket; position: number; bucketedAt: string; doneAt: string | null; createdAt: string }`
  - `type ActionResult<T> = { ok: true; data: T } | { ok: false; error: string }`
  - `STALE_AFTER_DAYS: number` (= `3`)
  - `isStale(task: Pick<Task, 'bucket' | 'bucketedAt' | 'doneAt'>, now: Date): boolean`

`isStale`은 현재 시각을 인자로 받는다. `Date.now()`를 안에서 부르면 테스트가 시계에 의존하게 된다.

- [ ] **Step 1: 타입 파일 작성**

`src/lib/tasks/types.ts`:

```typescript
export type Bucket = 'today' | 'week' | 'someday'

export const BUCKETS: Bucket[] = ['today', 'week', 'someday']

export const BUCKET_LABELS: Record<Bucket, string> = {
  today: '오늘',
  week: '이번주',
  someday: '언젠가',
}

export type Task = {
  id: string
  title: string
  bucket: Bucket
  /** 그룹 내 정렬값. 작을수록 위. */
  position: number
  /** 현재 bucket으로 들어온 시각 (ISO 8601) */
  bucketedAt: string
  /** null이면 미완료 */
  doneAt: string | null
  createdAt: string
}

export type ActionResult<T> = { ok: true; data: T } | { ok: false; error: string }
```

- [ ] **Step 2: 실패하는 테스트 작성**

`src/lib/staleness.test.ts`:

```typescript
import { describe, expect, it } from 'vitest'
import { isStale } from './staleness'

const NOW = new Date('2026-07-31T12:00:00Z')

function daysAgo(n: number): string {
  return new Date(NOW.getTime() - n * 24 * 60 * 60 * 1000).toISOString()
}

describe('isStale', () => {
  it('오늘 그룹에서 3일이 지났고 미완료면 오래된 것이다', () => {
    expect(isStale({ bucket: 'today', bucketedAt: daysAgo(3), doneAt: null }, NOW)).toBe(true)
  })

  it('3일이 안 됐으면 아니다', () => {
    expect(isStale({ bucket: 'today', bucketedAt: daysAgo(2), doneAt: null }, NOW)).toBe(false)
  })

  it('완료했으면 아무리 오래돼도 아니다', () => {
    expect(
      isStale({ bucket: 'today', bucketedAt: daysAgo(30), doneAt: daysAgo(1) }, NOW)
    ).toBe(false)
  })

  it('오늘 그룹이 아니면 아니다', () => {
    expect(isStale({ bucket: 'week', bucketedAt: daysAgo(30), doneAt: null }, NOW)).toBe(false)
    expect(isStale({ bucket: 'someday', bucketedAt: daysAgo(30), doneAt: null }, NOW)).toBe(false)
  })
})
```

- [ ] **Step 3: 테스트가 실패하는지 확인**

Run: `npm test`
Expected: FAIL — `Failed to resolve import "./staleness"`

- [ ] **Step 4: 최소 구현 작성**

`src/lib/staleness.ts`:

```typescript
import type { Task } from './tasks/types'

/** `today`에 이만큼 머무른 미완료 항목에 흐린 점을 표시한다. */
export const STALE_AFTER_DAYS = 3

const MS_PER_DAY = 24 * 60 * 60 * 1000

export function isStale(
  task: Pick<Task, 'bucket' | 'bucketedAt' | 'doneAt'>,
  now: Date
): boolean {
  if (task.bucket !== 'today') return false
  if (task.doneAt !== null) return false
  const elapsed = now.getTime() - new Date(task.bucketedAt).getTime()
  return elapsed >= STALE_AFTER_DAYS * MS_PER_DAY
}
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `npm test`
Expected: PASS, staleness 테스트 4개 + ranking 테스트 11개

- [ ] **Step 6: 커밋**

```powershell
git add -A
git commit -m "feat: add task types and staleness rule"
```

---

### Task 4: 데이터베이스 스키마와 RLS

**Files:**
- Create: `supabase/migrations/20260731000000_create_tasks.sql`

`supabase/config.toml`은 만들지 않는다. 로컬 스택도 CLI 링크도 쓰지 않으므로 아무도 그 파일을 읽지 않는다.

**Interfaces:**
- Consumes: `Bucket` 값 (`today` / `week` / `someday`)
- Produces: `public.tasks` 테이블. 컬럼: `id`, `user_id`, `title`, `bucket`, `position`, `bucketed_at`, `done_at`, `created_at`

**전제:** 원격 프로젝트 `cwxbzvevvoxdarekeldb`에 대시보드로 접근할 수 있어야 한다. Docker도 CLI 인증도 필요 없다.

이 Task의 SQL 실행과 사용자 생성은 **사람이 대시보드에서** 한다. 구현자는 파일을 만들고, 사람이 실행한 결과를 받아 검증한다.

- [ ] **Step 1: 마이그레이션 파일 작성**

CLI가 인증돼 있지 않으므로 파일명을 직접 짓는다. CLI가 쓰는 형식과 같게 맞춘다: `<YYYYMMDDHHMMSS>_<name>.sql`.

`supabase/migrations/20260731000000_create_tasks.sql`을 만들고 다음을 넣는다:

```sql
-- 할 일 하나가 도메인의 전부다. 테이블도 하나다.

create type public.task_bucket as enum ('today', 'week', 'someday');

create table public.tasks (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users (id) on delete cascade,
  -- trim() 은 기본값이 ASCII 공백(0x20)뿐이라 탭이나 줄바꿈만으로 된 제목을 통과시킨다.
  -- 잘라낼 문자를 명시해야 실제로 "내용 없는 제목"을 막는다.
  title text not null check (length(btrim(title, E' \t\n\r')) > 0),
  bucket public.task_bucket not null default 'today',
  -- 분수 순위. 정수가 아니라 실수여야 두 항목 사이에 끼워 넣을 수 있다.
  position double precision not null,
  -- 현재 bucket으로 들어온 시각. "오래됨" 표시를 계산하는 기준이다.
  bucketed_at timestamptz not null default now(),
  done_at timestamptz,
  created_at timestamptz not null default now()
);

-- 목록은 항상 (내 것) → (그룹별) → (순서대로) 로 읽는다.
create index tasks_user_bucket_position_idx
  on public.tasks (user_id, bucket, position);

alter table public.tasks enable row level security;

-- 정책마다 TO authenticated 와 소유권 조건을 함께 둔다.
-- TO authenticated 만으로는 로그인한 아무나 남의 행을 읽는다.
create policy "tasks_select_own" on public.tasks
  for select to authenticated
  using ((select auth.uid()) = user_id);

create policy "tasks_insert_own" on public.tasks
  for insert to authenticated
  with check ((select auth.uid()) = user_id);

-- UPDATE 는 USING 과 WITH CHECK 를 둘 다 가져야 한다.
-- WITH CHECK 가 없으면 행의 user_id 를 남에게 넘길 수 있다.
create policy "tasks_update_own" on public.tasks
  for update to authenticated
  using ((select auth.uid()) = user_id)
  with check ((select auth.uid()) = user_id);

create policy "tasks_delete_own" on public.tasks
  for delete to authenticated
  using ((select auth.uid()) = user_id);

-- Data API 노출. 새 테이블이 자동으로 노출되지 않는 설정일 수 있으므로 명시한다.
grant select, insert, update, delete on public.tasks to authenticated;
```

- [ ] **Step 2: 환경변수 파일 작성**

`.env.local`을 만든다. 이 파일은 git-ignore 되어 있으므로 커밋되지 않는다.

```
NEXT_PUBLIC_SUPABASE_URL=https://cwxbzvevvoxdarekeldb.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_0NCtH9t7_tbtV27W685pxg_I0h1uwCy
```

- [ ] **Step 3: 사람에게 SQL 실행 요청**

구현자는 여기서 멈추고, Step 1에서 쓴 SQL 전문을 사람에게 전달해 대시보드 SQL Editor에서 실행하도록 요청한다. 직접 실행할 방법은 없다.

실행 위치: `https://supabase.com/dashboard/project/cwxbzvevvoxdarekeldb/sql/new`

Expected: `Success. No rows returned`.

- [ ] **Step 4: RLS가 실제로 막는지 확인 요청**

같은 SQL Editor에서 다음을 실행해 달라고 요청하고, 결과를 받는다:

```sql
set role authenticated;
select * from public.tasks;
reset role;
```

Expected: 오류 없이 **0행**. `auth.uid()`가 없으므로 아무것도 안 보이는 것이 정상이다.
- `permission denied`가 나오면 `grant`가 빠진 것이다.
- 행이 보이면 RLS가 안 켜진 것이다. 둘 다 다음으로 넘어가면 안 되는 상태다.

- [ ] **Step 5: 테스트 계정 두 개 만들기 요청**

대시보드 Authentication > Users에서 계정 두 개를 만들어 달라고 요청한다. 둘 다 "Auto Confirm User"를 켠다.

| Email | Password | 용도 |
|---|---|---|
| `test@example.com` | `test-password-1234` | 통합 테스트와 E2E의 주 계정 |
| `other@example.com` | `other-password-1234` | RLS가 남의 행을 막는지 확인할 상대역 |

RLS 격리를 검증하려면 "남"이 실제로 한 명 있어야 한다. 계정 하나로는 그 테스트를 쓸 수 없다.

- [ ] **Step 6: 커밋**

`.env.local`이 커밋에 포함되지 않았는지 `git status`로 확인한 뒤 커밋한다.

```powershell
git add -A
git commit -m "feat: add tasks table with RLS policies"
```

---

### Task 5: Supabase 클라이언트와 로그인

**Files:**
- Create: `src/lib/supabase/client.ts`, `src/lib/supabase/server.ts`, `src/lib/supabase/proxy.ts`
- Create: `proxy.ts` (프로젝트 루트)
- Create: `src/app/login/page.tsx`, `src/app/login/actions.ts`
- Modify: `src/app/page.tsx`
- Create: `.env.local`

**Interfaces:**
- Consumes: Task 4의 로컬 Supabase 인스턴스
- Produces:
  - `createClient(): SupabaseClient` (브라우저, `@/lib/supabase/client`)
  - `createClient(): Promise<SupabaseClient>` (서버, `@/lib/supabase/server`) — **await 해야 한다**
  - `updateSession(request: NextRequest): Promise<NextResponse>`
  - `login(formData: FormData): Promise<void>` (Server Action)
  - `logout(): Promise<void>` (Server Action)

가입 폼은 만들지 않는다. 계정은 Studio에서 미리 만든 하나뿐이고, 가입 경로를 두지 않는 것이 남이 계정을 만들지 못하게 막는 가장 확실한 방법이다.

- [ ] **Step 1: 패키지 설치와 환경변수**

```powershell
npm install @supabase/supabase-js@^2.111.0 @supabase/ssr@^0.12.4
```

`@supabase/ssr`의 버전은 중요하다. `setAll(cookiesToSet, headers)`의 두 번째 인자는 0.8 이후에 생겼고, 여기에는 인증 쿠키를 실어 보내는 응답을 CDN이나 리버스 프록시가 캐시하지 못하게 막는 헤더가 담긴다 (`Cache-Control: private, no-cache, no-store...`). 이 헤더를 응답에 붙이지 않으면 **한 사용자의 세션 토큰이 캐시를 통해 다른 사용자에게 전달될 수 있다.** Vercel처럼 CDN 뒤에 배포하는 이상 이건 이론상의 위험이 아니다. 0.7.x는 이 인자를 아예 넘기지 않으므로 쓰지 않는다.

`.env.local`을 만들고 Task 4 Step 1에서 출력된 값을 넣는다. `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`에는 **anon/publishable 키**를 넣는다. `service_role` 키를 넣으면 브라우저에 유출된다.

```
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=<supabase start 가 출력한 anon key>
```

- [ ] **Step 2: 브라우저 클라이언트**

`src/lib/supabase/client.ts`:

```typescript
import { createBrowserClient } from '@supabase/ssr'

export const createClient = () =>
  createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!
  )
```

- [ ] **Step 3: 서버 클라이언트**

`src/lib/supabase/server.ts`:

```typescript
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

export const createClient = async () => {
  const cookieStore = await cookies()

  return createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!,
    {
      cookies: {
        getAll() {
          return cookieStore.getAll()
        },
        setAll(cookiesToSet) {
          try {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options)
            )
          } catch {
            // 서버 컴포넌트에서는 쿠키를 쓸 수 없다. proxy 가 갱신을 맡으므로 무시해도 된다.
          }
        },
      },
    }
  )
}
```

- [ ] **Step 4: 세션 갱신 로직**

`src/lib/supabase/proxy.ts`:

```typescript
import { createServerClient } from '@supabase/ssr'
import { NextResponse, type NextRequest } from 'next/server'

export async function updateSession(request: NextRequest) {
  let supabaseResponse = NextResponse.next({ request })

  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY!,
    {
      cookies: {
        getAll() {
          return request.cookies.getAll()
        },
        setAll(cookiesToSet, headers) {
          cookiesToSet.forEach(({ name, value }) => request.cookies.set(name, value))
          supabaseResponse = NextResponse.next({ request })
          cookiesToSet.forEach(({ name, value, options }) =>
            supabaseResponse.cookies.set(name, value, options)
          )
          Object.entries(headers).forEach(([key, value]) =>
            supabaseResponse.headers.set(key, value)
          )
        },
      },
    }
  )

  // createServerClient 와 getClaims() 사이에 아무 코드도 넣지 말 것.
  // 여기에 무언가를 끼워 넣으면 사용자가 무작위로 로그아웃되는,
  // 원인을 찾기 매우 어려운 버그가 생긴다.
  await supabase.auth.getClaims()

  return supabaseResponse
}
```

- [ ] **Step 5: 루트 proxy.ts**

Next.js 16에서는 파일 이름이 `middleware.ts`가 아니라 `proxy.ts`이고, 내보내는 함수 이름도 `proxy`다.

`proxy.ts` (프로젝트 루트, `src/` 밖):

```typescript
import { type NextRequest } from 'next/server'
import { updateSession } from '@/lib/supabase/proxy'

export async function proxy(request: NextRequest) {
  return await updateSession(request)
}

export const config = {
  matcher: [
    '/((?!_next/static|_next/image|favicon.ico|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)',
  ],
}
```

- [ ] **Step 6: 로그인 액션**

`src/app/login/actions.ts`:

```typescript
'use server'

import { revalidatePath } from 'next/cache'
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'

export async function login(formData: FormData) {
  const supabase = await createClient()

  const { error } = await supabase.auth.signInWithPassword({
    email: String(formData.get('email') ?? ''),
    password: String(formData.get('password') ?? ''),
  })

  if (error) {
    redirect('/login?error=1')
  }

  revalidatePath('/', 'layout')
  redirect('/')
}

export async function logout() {
  const supabase = await createClient()
  await supabase.auth.signOut()
  revalidatePath('/', 'layout')
  redirect('/login')
}
```

- [ ] **Step 7: 로그인 페이지**

`src/app/login/page.tsx`:

```tsx
import { login } from './actions'

export default async function LoginPage({
  searchParams,
}: {
  searchParams: Promise<{ error?: string }>
}) {
  const { error } = await searchParams

  return (
    <main className="mx-auto flex min-h-dvh max-w-sm flex-col justify-center gap-4 p-6">
      <h1 className="text-lg font-medium">순서</h1>
      <form action={login} className="flex flex-col gap-3">
        <input
          name="email"
          type="email"
          required
          placeholder="이메일"
          autoComplete="username"
          className="rounded border px-3 py-2"
        />
        <input
          name="password"
          type="password"
          required
          placeholder="비밀번호"
          autoComplete="current-password"
          className="rounded border px-3 py-2"
        />
        <button type="submit" className="rounded bg-neutral-900 px-3 py-2 text-white">
          로그인
        </button>
      </form>
      {error ? (
        <p role="alert" className="text-sm text-red-600">
          이메일 또는 비밀번호가 맞지 않습니다.
        </p>
      ) : null}
    </main>
  )
}
```

- [ ] **Step 8: 홈을 보호된 페이지로 바꾸기**

`src/app/page.tsx` 전체를 다음으로 교체한다:

```tsx
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'

export default async function HomePage() {
  const supabase = await createClient()
  const { data } = await supabase.auth.getClaims()

  if (!data?.claims) {
    redirect('/login')
  }

  return <main className="p-6">로그인됨: {String(data.claims.email)}</main>
}
```

- [ ] **Step 9: 수동 확인**

Run: `npm run dev`

1. `http://localhost:3000` 접속 → `/login`으로 넘어간다
2. `test@example.com` / `test-password-1234`로 로그인 → 홈에 `로그인됨: test@example.com`이 보인다
3. 브라우저를 새로고침해도 로그인이 유지된다
4. 일부러 틀린 비밀번호를 넣으면 오류 문구가 보인다

네 가지가 모두 확인되면 Ctrl+C.

- [ ] **Step 10: 커밋**

```powershell
git add -A
git commit -m "feat: add Supabase clients and password login"
```

---

### Task 6: 읽기 계층 (`lib/tasks/queries.ts`)

**Files:**
- Create: `src/lib/tasks/queries.ts`
- Test: `src/lib/tasks/queries.test.ts`

**Interfaces:**
- Consumes: `Task`, `Bucket` (`@/lib/tasks/types`)
- Produces:
  - `type TaskRow` — DB 행 모양 (snake_case)
  - `rowToTask(row: TaskRow): Task` — **snake_case ↔ camelCase 변환이 일어나는 유일한 곳**
  - `listTasks(supabase: SupabaseClient): Promise<Task[]>`

검색용 질의는 만들지 않는다. 혼자 쓰는 앱의 항목 수는 클라이언트에서 거르기에 충분히 적고, 그 편이 왕복 없이 즉각적이다.

- [ ] **Step 1: 실패하는 테스트 작성**

DB 없이 검증할 수 있는 것은 변환 함수다. 여기에 테스트를 둔다.

`src/lib/tasks/queries.test.ts`:

```typescript
import { describe, expect, it } from 'vitest'
import { rowToTask } from './queries'

describe('rowToTask', () => {
  it('snake_case 행을 camelCase Task 로 바꾼다', () => {
    expect(
      rowToTask({
        id: 'a1',
        user_id: 'u1',
        title: '진료기록 정리',
        bucket: 'today',
        position: 1.5,
        bucketed_at: '2026-07-31T00:00:00Z',
        done_at: null,
        created_at: '2026-07-30T00:00:00Z',
      })
    ).toEqual({
      id: 'a1',
      title: '진료기록 정리',
      bucket: 'today',
      position: 1.5,
      bucketedAt: '2026-07-31T00:00:00Z',
      doneAt: null,
      createdAt: '2026-07-30T00:00:00Z',
    })
  })

  it('user_id 는 클라이언트로 넘기지 않는다', () => {
    const task = rowToTask({
      id: 'a1',
      user_id: 'u1',
      title: 'x',
      bucket: 'week',
      position: 0,
      bucketed_at: '2026-07-31T00:00:00Z',
      done_at: '2026-07-31T01:00:00Z',
      created_at: '2026-07-30T00:00:00Z',
    })
    expect(task).not.toHaveProperty('user_id')
    expect(task.doneAt).toBe('2026-07-31T01:00:00Z')
  })
})
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `npm test`
Expected: FAIL — `Failed to resolve import "./queries"`

- [ ] **Step 3: 구현 작성**

`src/lib/tasks/queries.ts`:

```typescript
import type { SupabaseClient } from '@supabase/supabase-js'
import type { Bucket, Task } from './types'

/** DB 행 모양. 이 파일 밖으로 새어 나가지 않는다. */
export type TaskRow = {
  id: string
  user_id: string
  title: string
  bucket: Bucket
  position: number
  bucketed_at: string
  done_at: string | null
  created_at: string
}

const COLUMNS = 'id, user_id, title, bucket, position, bucketed_at, done_at, created_at'

/** snake_case ↔ camelCase 변환이 일어나는 유일한 지점. */
export function rowToTask(row: TaskRow): Task {
  return {
    id: row.id,
    title: row.title,
    bucket: row.bucket,
    position: row.position,
    bucketedAt: row.bucketed_at,
    doneAt: row.done_at,
    createdAt: row.created_at,
  }
}

/** 내 할 일 전부. 그룹 순서는 클라이언트가 정하므로 여기서는 position 만 정렬한다. */
export async function listTasks(supabase: SupabaseClient): Promise<Task[]> {
  const { data, error } = await supabase
    .from('tasks')
    .select(COLUMNS)
    .order('position', { ascending: true })

  if (error) throw error
  return (data as TaskRow[]).map(rowToTask)
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test`
Expected: PASS

- [ ] **Step 5: 커밋**

```powershell
git add -A
git commit -m "feat: add task read queries"
```

---

### Task 7: 쓰기 계층 (Server Actions)

**Files:**
- Create: `src/lib/tasks/actions.ts`

**Interfaces:**
- Consumes: `createClient` (`@/lib/supabase/server`), `ActionResult`, `Bucket`, `Task`
- Produces (전부 Server Action, 전부 `ActionResult<null>` 반환):
  - `addTask(input: { id: string; title: string; position: number })`
  - `setDone(input: { id: string; done: boolean })`
  - `moveTask(input: { id: string; bucket: Bucket; position: number })`
  - `renameTask(input: { id: string; title: string })`
  - `deleteTask(input: { id: string })`
  - `restoreTask(input: { task: Task })`
  - `rebalanceGroup(input: { positions: { id: string; position: number }[] })`

`id`를 클라이언트가 만들어 넘기는 것이 핵심이다. 그래야 낙관적으로 화면에 그린 항목과 서버에 저장된 항목의 id가 같아서, 응답을 받고 자리를 맞바꾸는 조정 로직이 필요 없다.

`moveTask`는 현재 `bucket`을 먼저 읽는다. 그룹이 실제로 바뀐 경우에만 `bucketed_at`을 갱신해야 하는데, 이 판단을 클라이언트에 맡기면 시각을 클라이언트가 정하게 된다. 드래그는 자주 일어나는 동작이 아니므로 조회 한 번을 더 하는 편이 낫다.

- [ ] **Step 1: 구현 작성**

`src/lib/tasks/actions.ts`:

```typescript
'use server'

import { createClient } from '@/lib/supabase/server'
import type { ActionResult, Bucket, Task } from './types'

const SAVE_FAILED = '저장하지 못했습니다'

/** 로그인한 사용자의 id. RLS 가 최종 방어선이지만, 여기서 먼저 막아 의미 있는 오류를 준다. */
async function requireUserId(supabase: Awaited<ReturnType<typeof createClient>>) {
  const { data } = await supabase.auth.getClaims()
  const userId = data?.claims?.sub
  if (typeof userId !== 'string') return null
  return userId
}

export async function addTask(input: {
  id: string
  title: string
  position: number
}): Promise<ActionResult<null>> {
  const title = input.title.trim()
  if (title.length === 0) return { ok: false, error: '내용이 비어 있습니다' }

  const supabase = await createClient()
  const userId = await requireUserId(supabase)
  if (!userId) return { ok: false, error: '로그인이 필요합니다' }

  const { error } = await supabase.from('tasks').insert({
    id: input.id,
    user_id: userId,
    title,
    bucket: 'today',
    position: input.position,
  })

  return error ? { ok: false, error: SAVE_FAILED } : { ok: true, data: null }
}

export async function setDone(input: {
  id: string
  done: boolean
}): Promise<ActionResult<null>> {
  const supabase = await createClient()

  const { error } = await supabase
    .from('tasks')
    .update({ done_at: input.done ? new Date().toISOString() : null })
    .eq('id', input.id)

  return error ? { ok: false, error: SAVE_FAILED } : { ok: true, data: null }
}

export async function moveTask(input: {
  id: string
  bucket: Bucket
  position: number
}): Promise<ActionResult<null>> {
  const supabase = await createClient()

  // 그룹이 실제로 바뀔 때만 bucketed_at 을 갱신한다.
  // 그룹 안에서 순서만 바꾼 것까지 "오래됨" 시계를 되돌리면 안 된다.
  const { data: current, error: readError } = await supabase
    .from('tasks')
    .select('bucket')
    .eq('id', input.id)
    .single()

  if (readError || !current) return { ok: false, error: SAVE_FAILED }

  const patch: Record<string, unknown> = {
    bucket: input.bucket,
    position: input.position,
  }
  if (current.bucket !== input.bucket) {
    patch.bucketed_at = new Date().toISOString()
  }

  const { error } = await supabase.from('tasks').update(patch).eq('id', input.id)

  return error ? { ok: false, error: SAVE_FAILED } : { ok: true, data: null }
}

export async function renameTask(input: {
  id: string
  title: string
}): Promise<ActionResult<null>> {
  const title = input.title.trim()
  if (title.length === 0) return { ok: false, error: '내용이 비어 있습니다' }

  const supabase = await createClient()
  const { error } = await supabase.from('tasks').update({ title }).eq('id', input.id)

  return error ? { ok: false, error: SAVE_FAILED } : { ok: true, data: null }
}

export async function deleteTask(input: { id: string }): Promise<ActionResult<null>> {
  const supabase = await createClient()
  const { error } = await supabase.from('tasks').delete().eq('id', input.id)

  return error ? { ok: false, error: SAVE_FAILED } : { ok: true, data: null }
}

/** 삭제 직후 "실행 취소"를 눌렀을 때 같은 id 로 되살린다. */
export async function restoreTask(input: { task: Task }): Promise<ActionResult<null>> {
  const supabase = await createClient()
  const userId = await requireUserId(supabase)
  if (!userId) return { ok: false, error: '로그인이 필요합니다' }

  const { error } = await supabase.from('tasks').insert({
    id: input.task.id,
    user_id: userId,
    title: input.task.title,
    bucket: input.task.bucket,
    position: input.task.position,
    bucketed_at: input.task.bucketedAt,
    done_at: input.task.doneAt,
    created_at: input.task.createdAt,
  })

  return error ? { ok: false, error: SAVE_FAILED } : { ok: true, data: null }
}

/**
 * 한 그룹의 순위를 1 간격으로 다시 편다.
 * 같은 두 항목 사이에 수십 번 끼워 넣어 부동소수점 정밀도가 고갈됐을 때만 부른다.
 */
export async function rebalanceGroup(input: {
  positions: { id: string; position: number }[]
}): Promise<ActionResult<null>> {
  const supabase = await createClient()

  for (const { id, position } of input.positions) {
    const { error } = await supabase.from('tasks').update({ position }).eq('id', id)
    if (error) return { ok: false, error: SAVE_FAILED }
  }

  return { ok: true, data: null }
}
```

`eq('id', ...)`만 걸고 `user_id`를 걸지 않는 것은 의도적이다. RLS의 `USING` 절이 남의 행을 이미 걸러내므로, 여기서 조건을 중복해 봐야 두 곳이 어긋날 여지만 늘어난다.

- [ ] **Step 2: 타입 검사**

Run: `npx tsc --noEmit`
Expected: 오류 없음

- [ ] **Step 3: 커밋**

```powershell
git add -A
git commit -m "feat: add task write server actions"
```

---

### Task 7B: 통합 테스트 — RLS와 읽기 계층

단위 테스트는 `rowToTask`가 필드를 옳게 옮기는지까지만 말해 준다. 정책이 실제로 남의 행을 막는지는 진짜 데이터베이스에 물어봐야 안다. **보안 경계를 검증하는 유일한 테스트이므로 건너뛰지 않는다.**

Server Action 자체(`'use server'` 함수)는 Next.js의 요청 컨텍스트가 있어야 돌아서 Vitest에서 직접 부르기 어렵다. 대신 그 아래 계층 — 테이블, 정책, `listTasks` — 을 실제 클라이언트로 검증한다. 이 층이 통과하면 Server Action에 남는 위험은 얇은 배선뿐이고, 그건 Task 13의 E2E가 훑는다.

**Files:**
- Create: `src/lib/tasks/queries.integration.test.ts`
- Create: `.env.test`
- Modify: `vitest.config.ts`, `package.json`, `.gitignore`

**Interfaces:**
- Consumes: `listTasks`, `rowToTask`, Task 4의 두 계정
- Produces: `npm run test:integration`

**전제:** Task 4에서 원격 프로젝트에 테이블과 두 계정이 만들어져 있어야 한다.

**이 테스트는 실제 프로젝트의 데이터베이스에 쓴다.** 그러므로 두 가지를 지킨다. 첫째, 만든 행은 `afterAll`에서 반드시 지운다. 둘째, 삭제는 이 테스트가 만든 id 목록으로만 한다 — `delete()` 뒤에 `.in('id', createdIds)` 외의 조건을 쓰지 않는다. 조건 없는 삭제나 제목 패턴으로 지우는 삭제는 실제 데이터를 날릴 수 있다.

- [ ] **Step 1: dotenv 설치와 환경 파일**

```powershell
npm install --save-dev dotenv@^16
```

`.env.test`:

```
SUPABASE_TEST_URL=https://cwxbzvevvoxdarekeldb.supabase.co
SUPABASE_TEST_ANON_KEY=sb_publishable_0NCtH9t7_tbtV27W685pxg_I0h1uwCy
```

`.gitignore`에 `.env.test`를 추가한다.

- [ ] **Step 2: Vitest가 통합 테스트를 따로 돌게 설정**

통합 테스트는 네트워크와 실제 프로젝트를 쓰므로 `npm test`에 섞지 않는다.

`vitest.config.ts`를 다음으로 교체한다:

```typescript
import { config } from 'dotenv'
import { defineConfig } from 'vitest/config'

config({ path: '.env.test' })

const integration = process.env.VITEST_INTEGRATION === '1'

export default defineConfig({
  test: {
    environment: 'node',
    include: integration
      ? ['src/**/*.integration.test.ts']
      : ['src/**/*.test.ts'],
    exclude: integration ? [] : ['**/node_modules/**', '**/*.integration.test.ts'],
  },
})
```

`package.json`의 `scripts`에 추가한다:

```json
"test:integration": "cross-env VITEST_INTEGRATION=1 vitest run"
```

Windows에서 인라인 환경변수 지정이 안 되므로 `cross-env`를 쓴다:

```powershell
npm install --save-dev cross-env@^7
```

- [ ] **Step 3: 실패하는 테스트 작성**

`src/lib/tasks/queries.integration.test.ts`:

```typescript
import { createClient, type SupabaseClient } from '@supabase/supabase-js'
import { afterAll, beforeAll, describe, expect, it } from 'vitest'
import { listTasks } from './queries'

const URL = process.env.SUPABASE_TEST_URL!
const KEY = process.env.SUPABASE_TEST_ANON_KEY!

async function signIn(email: string, password: string): Promise<SupabaseClient> {
  const client = createClient(URL, KEY)
  const { error } = await client.auth.signInWithPassword({ email, password })
  if (error) throw new Error(`로그인 실패 (${email}): ${error.message}`)
  return client
}

let me: SupabaseClient
let other: SupabaseClient
let myUserId: string
const createdIds: string[] = []

beforeAll(async () => {
  me = await signIn('test@example.com', 'test-password-1234')
  other = await signIn('other@example.com', 'other-password-1234')
  const { data } = await me.auth.getUser()
  myUserId = data.user!.id
})

afterAll(async () => {
  if (createdIds.length > 0) {
    await me.from('tasks').delete().in('id', createdIds)
  }
})

async function insertMine(title: string, position: number): Promise<string> {
  const { data, error } = await me
    .from('tasks')
    .insert({ user_id: myUserId, title, position })
    .select('id')
    .single()
  if (error) throw error
  createdIds.push(data.id)
  return data.id
}

describe('tasks 테이블', () => {
  it('내가 넣은 행을 내가 읽는다', async () => {
    const title = `통합 ${Date.now()}`
    await insertMine(title, 0)

    const tasks = await listTasks(me)
    const found = tasks.find((t) => t.title === title)

    expect(found).toBeDefined()
    expect(found!.bucket).toBe('today')
    expect(found!.doneAt).toBeNull()
    // rowToTask 가 실제 행에서도 camelCase 로 옮기는지 확인한다.
    expect(typeof found!.bucketedAt).toBe('string')
  })

  it('남의 행은 보이지 않는다', async () => {
    const title = `비밀 ${Date.now()}`
    await insertMine(title, 1)

    const theirs = await listTasks(other)

    expect(theirs.find((t) => t.title === title)).toBeUndefined()
  })

  it('남의 행은 고칠 수 없다', async () => {
    const id = await insertMine(`남의것 ${Date.now()}`, 2)

    const { data } = await other.from('tasks').update({ title: '탈취' }).eq('id', id).select()

    // RLS 의 USING 절이 행을 걸러내므로 오류가 아니라 0행이 영향받는다.
    expect(data).toEqual([])
  })

  it('행의 주인을 남에게 넘길 수 없다', async () => {
    const id = await insertMine(`이전 ${Date.now()}`, 3)
    const { data: otherUser } = await other.auth.getUser()

    const { error } = await me
      .from('tasks')
      .update({ user_id: otherUser.user!.id })
      .eq('id', id)

    // UPDATE 정책의 WITH CHECK 가 막아야 한다.
    expect(error).not.toBeNull()
  })

  it('빈 제목은 넣을 수 없다', async () => {
    const { data, error } = await me
      .from('tasks')
      .insert({ user_id: myUserId, title: '   ', position: 9 })
      .select('id')

    // 이 삽입은 실패해야 정상이다. 하지만 제약조건이 깨져서 성공해 버리면,
    // 그 행의 id 를 여기서 잡아두지 않는 한 정리 대상에서 빠져 실제 프로젝트에 남는다.
    // 정리가 가장 필요한 순간이 바로 이 테스트가 실패하는 순간이다.
    if (data) {
      createdIds.push(...data.map((row) => row.id))
    }

    expect(error).not.toBeNull()
  })
})
```

- [ ] **Step 4: 테스트 실행**

Run: `npm run test:integration`
Expected: PASS, 5개 통과

만약 "남의 행은 고칠 수 없다"가 실패하고 데이터가 실제로 바뀌었다면, Task 4의 `tasks_update_own` 정책이 잘못 적용된 것이다. 진행하지 말고 정책부터 고친다.

- [ ] **Step 5: 단위 테스트가 여전히 분리돼 도는지 확인**

Run: `npm test`
Expected: PASS. 통합 테스트는 **실행되지 않는다** (`.integration.test.ts`가 제외된다).

- [ ] **Step 6: 커밋**

```powershell
git add -A
git commit -m "test: verify RLS isolation against local Supabase"
```

---

### Task 8: TaskBoard와 입력창 — 낙관적 추가

화면에 실제로 무언가가 나타나는 첫 Task다. 이 Task가 끝나면 한 줄을 쳐서 엔터를 누르면 목록에 즉시 뜨고, 새로고침해도 남아 있다.

**Files:**
- Create: `src/components/TaskBoard.tsx`
- Create: `src/components/QuickAdd.tsx`
- Modify: `src/app/page.tsx`
- Modify: `src/app/layout.tsx` (한국어 로케일)

**Interfaces:**
- Consumes: `listTasks`, `addTask`, `rankBetween`, `Task`, `BUCKETS`, `BUCKET_LABELS`
- Produces:
  - `<TaskBoard initialTasks={Task[]} />`
  - `<QuickAdd onSubmit={(title: string) => void} />`

낙관적 상태를 다룰 때 스냅샷을 `useState`의 현재 값에서 읽으면 연속 조작에서 낡은 값을 잡는다. `useRef`를 단일 진실 원천으로 두고 동기적으로 갱신한다.

- [ ] **Step 1: QuickAdd 작성**

`src/components/QuickAdd.tsx`:

```tsx
'use client'

import { useEffect, useState } from 'react'

const DRAFT_KEY = 'sunseo:draft'

export function QuickAdd({ onSubmit }: { onSubmit: (title: string) => void }) {
  const [value, setValue] = useState('')

  // 세션이 만료돼 로그인 화면으로 튕겨도 치고 있던 문장은 잃지 않아야 한다.
  useEffect(() => {
    const saved = localStorage.getItem(DRAFT_KEY)
    if (saved) setValue(saved)
  }, [])

  useEffect(() => {
    if (value) localStorage.setItem(DRAFT_KEY, value)
    else localStorage.removeItem(DRAFT_KEY)
  }, [value])

  return (
    <form
      onSubmit={(e) => {
        e.preventDefault()
        const title = value.trim()
        // 빈 제목은 아무 일도 하지 않는다. 오류 메시지도 띄우지 않는다.
        if (title.length === 0) return
        onSubmit(title)
        setValue('')
      }}
    >
      <input
        // 열자마자 타이핑할 수 있어야 한다. 입력 마찰이 이 앱의 존재 이유다.
        autoFocus
        value={value}
        onChange={(e) => setValue(e.target.value)}
        placeholder="할 일 입력..."
        aria-label="할 일 입력"
        className="w-full rounded border px-3 py-2 outline-none focus:border-neutral-900"
      />
    </form>
  )
}
```

- [ ] **Step 2: TaskBoard 작성**

`src/components/TaskBoard.tsx`:

```tsx
'use client'

import { useCallback, useRef, useState } from 'react'
import { rankBetween } from '@/lib/ranking'
import { addTask } from '@/lib/tasks/actions'
import { BUCKETS, BUCKET_LABELS, type ActionResult, type Task } from '@/lib/tasks/types'
import { QuickAdd } from './QuickAdd'

export function TaskBoard({ initialTasks }: { initialTasks: Task[] }) {
  const [tasks, setTasks] = useState<Task[]>(initialTasks)
  const [error, setError] = useState<string | null>(null)

  // 낙관적 갱신에서 롤백하려면 "직전 상태"를 정확히 알아야 한다.
  // useState 의 값은 연속 조작 중 낡을 수 있으므로 ref 를 진실 원천으로 둔다.
  const tasksRef = useRef<Task[]>(initialTasks)

  const apply = useCallback((updater: (prev: Task[]) => Task[]) => {
    tasksRef.current = updater(tasksRef.current)
    setTasks(tasksRef.current)
  }, [])

  const mutate = useCallback(
    async (updater: (prev: Task[]) => Task[], run: () => Promise<ActionResult<null>>) => {
      const snapshot = tasksRef.current
      apply(updater)
      const result = await run()
      if (!result.ok) {
        tasksRef.current = snapshot
        setTasks(snapshot)
        setError(result.error)
      }
    },
    [apply]
  )

  const handleAdd = useCallback(
    (title: string) => {
      // id 를 클라이언트가 만든다. 낙관적으로 그린 항목과 저장된 항목의 id 가
      // 같으므로 응답을 받고 자리를 맞바꾸는 조정이 필요 없다.
      const id = crypto.randomUUID()
      const todayPositions = tasksRef.current
        .filter((t) => t.bucket === 'today')
        .map((t) => t.position)
      const last = todayPositions.length > 0 ? Math.max(...todayPositions) : null
      const position = rankBetween(last, null)
      const now = new Date().toISOString()

      const optimistic: Task = {
        id,
        title,
        bucket: 'today',
        position,
        bucketedAt: now,
        doneAt: null,
        createdAt: now,
      }

      void mutate(
        (prev) => [...prev, optimistic],
        () => addTask({ id, title, position })
      )
    },
    [mutate]
  )

  return (
    <div className="mx-auto flex max-w-xl flex-col gap-6 p-4">
      <QuickAdd onSubmit={handleAdd} />

      {BUCKETS.map((bucket) => {
        const items = tasks
          .filter((t) => t.bucket === bucket && t.doneAt === null)
          .sort((a, b) => a.position - b.position)

        return (
          <section key={bucket} className="flex flex-col gap-1">
            <h2 className="flex justify-between text-sm text-neutral-500">
              <span>{BUCKET_LABELS[bucket]}</span>
              <span>{items.length}</span>
            </h2>
            <ul>
              {items.map((task) => (
                <li key={task.id} className="py-1">
                  {task.title}
                </li>
              ))}
            </ul>
          </section>
        )
      })}

      {error ? (
        <p role="alert" className="text-sm text-red-600">
          {error}
        </p>
      ) : null}
    </div>
  )
}
```

- [ ] **Step 3: 홈 페이지를 목록 화면으로 바꾸기**

`src/app/page.tsx` 전체를 교체한다:

```tsx
import { redirect } from 'next/navigation'
import { TaskBoard } from '@/components/TaskBoard'
import { createClient } from '@/lib/supabase/server'
import { listTasks } from '@/lib/tasks/queries'

export default async function HomePage() {
  const supabase = await createClient()
  const { data } = await supabase.auth.getClaims()

  if (!data?.claims) {
    redirect('/login')
  }

  const tasks = await listTasks(supabase)

  return <TaskBoard initialTasks={tasks} />
}
```

- [ ] **Step 4: 초기 로드 실패 처리**

`listTasks`가 던지면 홈 화면이 통째로 죽는다. 목록 자리에 오류와 새로고침 버튼을 둔다.

`src/app/error.tsx`:

```tsx
'use client'

export default function Error({ reset }: { error: Error; reset: () => void }) {
  return (
    <main className="mx-auto flex max-w-xl flex-col items-start gap-3 p-6">
      <p>목록을 불러오지 못했습니다.</p>
      <button
        type="button"
        onClick={reset}
        className="rounded bg-neutral-900 px-3 py-2 text-sm text-white"
      >
        다시 시도
      </button>
    </main>
  )
}
```

- [ ] **Step 5: 로케일 설정**

`src/app/layout.tsx`에서 `<html lang="en">`을 `<html lang="ko">`로 바꾸고, `metadata`의 `title`을 `'순서'`, `description`을 `'개인 할 일 관리'`로 바꾼다.

- [ ] **Step 6: 수동 확인**

Run: `npm run dev`

1. 로그인 후 홈 → 입력창에 커서가 이미 있다
2. "진료기록 정리" 입력 후 엔터 → 즉시 "오늘" 아래에 나타나고 입력창이 비워진다
3. 연달아 두 개 더 입력 → 입력한 순서대로 아래로 쌓인다
4. 새로고침 → 세 개가 그대로 있다
5. 입력창에 글자를 치다 말고 새로고침 → 치던 글자가 남아 있다

- [ ] **Step 7: 커밋**

```powershell
git add -A
git commit -m "feat: add task board with optimistic quick add"
```

---

### Task 9: 항목 조작 — 완료, 편집, 삭제, 실행취소

**Files:**
- Create: `src/components/TaskItem.tsx`
- Create: `src/components/CompletedSection.tsx`
- Create: `src/components/Toast.tsx`
- Modify: `src/components/TaskBoard.tsx`

**Interfaces:**
- Consumes: `setDone`, `renameTask`, `deleteTask`, `restoreTask`, `isStale`
- Produces:
  - `<TaskItem task={Task} stale={boolean} onToggle onRename onDelete />`
  - `<CompletedSection tasks={Task[]} onToggle />`
  - `<Toast message={string} actionLabel?={string} onAction?={() => void} onDismiss={() => void} />`

- [ ] **Step 1: Toast 작성**

`src/components/Toast.tsx`:

```tsx
'use client'

import { useEffect } from 'react'

export function Toast({
  message,
  actionLabel,
  onAction,
  onDismiss,
  timeoutMs = 5000,
}: {
  message: string
  actionLabel?: string
  onAction?: () => void
  onDismiss: () => void
  timeoutMs?: number
}) {
  // onDismiss 는 호출부에서 useCallback 으로 고정된 참조여야 한다.
  // 렌더마다 새 함수가 들어오면 이 효과가 매번 다시 걸리면서 5초 타이머가
  // 계속 처음부터 다시 시작되고, 토스트가 영영 사라지지 않는다.
  useEffect(() => {
    const timer = setTimeout(onDismiss, timeoutMs)
    return () => clearTimeout(timer)
  }, [onDismiss, timeoutMs, message])

  return (
    <div
      role="status"
      className="fixed bottom-4 left-1/2 flex -translate-x-1/2 items-center gap-3 rounded bg-neutral-900 px-4 py-2 text-sm text-white shadow-lg"
    >
      <span>{message}</span>
      {actionLabel && onAction ? (
        <button type="button" onClick={onAction} className="underline">
          {actionLabel}
        </button>
      ) : null}
    </div>
  )
}
```

- [ ] **Step 2: TaskItem 작성**

`src/components/TaskItem.tsx`:

```tsx
'use client'

import { useState } from 'react'
import type { Task } from '@/lib/tasks/types'

export function TaskItem({
  task,
  stale,
  onToggle,
  onRename,
  onDelete,
}: {
  task: Task
  stale: boolean
  onToggle: (done: boolean) => void
  onRename: (title: string) => void
  onDelete: () => void
}) {
  const [editing, setEditing] = useState(false)
  const [draft, setDraft] = useState(task.title)

  function commit() {
    setEditing(false)
    const next = draft.trim()
    if (next.length === 0 || next === task.title) {
      setDraft(task.title)
      return
    }
    onRename(next)
  }

  return (
    <li className="group flex items-center gap-2 py-1">
      <input
        type="checkbox"
        checked={task.doneAt !== null}
        onChange={(e) => onToggle(e.target.checked)}
        aria-label={`${task.title} 완료`}
      />

      {editing ? (
        <input
          autoFocus
          value={draft}
          onChange={(e) => setDraft(e.target.value)}
          onBlur={commit}
          onKeyDown={(e) => {
            if (e.key === 'Enter') commit()
            if (e.key === 'Escape') {
              setDraft(task.title)
              setEditing(false)
            }
          }}
          className="flex-1 border-b outline-none"
        />
      ) : (
        <button
          type="button"
          // 편집을 열 때마다 현재 제목에서 다시 시작한다. draft 는 이 컴포넌트가
          // 들고 있으므로, 저장이 실패해 제목이 롤백되면 draft 에는 실패한 문구가
          // 남는다. 그대로 두면 다음에 열었을 때 그 문구가 보인다.
          onClick={() => {
            setDraft(task.title)
            setEditing(true)
          }}
          className={`flex-1 text-left ${task.doneAt ? 'text-neutral-400 line-through' : ''}`}
        >
          {task.title}
        </button>
      )}

      {/* 오래된 항목 표시. 위치는 바꾸지 않고 흐린 점만 붙인다. */}
      {stale ? (
        <span aria-label="사흘 넘게 남아 있음" className="text-neutral-300">
          ·
        </span>
      ) : null}

      <button
        type="button"
        onClick={onDelete}
        aria-label={`${task.title} 삭제`}
        className="text-neutral-300 opacity-0 transition group-hover:opacity-100 focus:opacity-100"
      >
        ×
      </button>
    </li>
  )
}
```

- [ ] **Step 3: CompletedSection 작성**

`src/components/CompletedSection.tsx`:

```tsx
'use client'

import { useState } from 'react'
import type { Task } from '@/lib/tasks/types'

export function CompletedSection({
  tasks,
  onToggle,
}: {
  tasks: Task[]
  onToggle: (id: string, done: boolean) => void
}) {
  const [open, setOpen] = useState(false)

  const todayCount = tasks.filter((t) => {
    if (!t.doneAt) return false
    const done = new Date(t.doneAt)
    const now = new Date()
    return done.toDateString() === now.toDateString()
  }).length

  if (tasks.length === 0) return null

  return (
    <section>
      <button
        type="button"
        onClick={() => setOpen((v) => !v)}
        aria-expanded={open}
        className="text-sm text-neutral-500"
      >
        {open ? '▾' : '▸'} 완료 (오늘 {todayCount}개)
      </button>
      {open ? (
        <ul className="mt-1">
          {tasks.map((task) => (
            <li key={task.id} className="flex items-center gap-2 py-1">
              <input
                type="checkbox"
                checked
                onChange={() => onToggle(task.id, false)}
                aria-label={`${task.title} 완료 취소`}
              />
              <span className="text-neutral-400 line-through">{task.title}</span>
            </li>
          ))}
        </ul>
      ) : null}
    </section>
  )
}
```

- [ ] **Step 4: TaskBoard에 조작 연결**

`src/components/TaskBoard.tsx`를 다음으로 교체한다:

```tsx
'use client'

import { useCallback, useRef, useState } from 'react'
import { rankBetween } from '@/lib/ranking'
import { isStale } from '@/lib/staleness'
import {
  addTask,
  deleteTask,
  renameTask,
  restoreTask,
  setDone,
} from '@/lib/tasks/actions'
import { BUCKETS, BUCKET_LABELS, type ActionResult, type Task } from '@/lib/tasks/types'
import { CompletedSection } from './CompletedSection'
import { QuickAdd } from './QuickAdd'
import { TaskItem } from './TaskItem'
import { Toast } from './Toast'

type ToastState = {
  message: string
  actionLabel?: string
  onAction?: () => void
}

export function TaskBoard({ initialTasks }: { initialTasks: Task[] }) {
  const [tasks, setTasks] = useState<Task[]>(initialTasks)
  const [toast, setToast] = useState<ToastState | null>(null)
  const tasksRef = useRef<Task[]>(initialTasks)

  const apply = useCallback((updater: (prev: Task[]) => Task[]) => {
    tasksRef.current = updater(tasksRef.current)
    setTasks(tasksRef.current)
  }, [])

  // Toast 의 자동 사라짐 타이머가 이 참조에 걸린다. 렌더마다 새로 만들면
  // 타이머가 매번 리셋되어 토스트가 사라지지 않는다. 반드시 고정해 둔다.
  const dismissToast = useCallback(() => setToast(null), [])

  const mutate = useCallback(
    async (updater: (prev: Task[]) => Task[], run: () => Promise<ActionResult<null>>) => {
      const snapshot = tasksRef.current
      apply(updater)
      const result = await run()
      if (!result.ok) {
        tasksRef.current = snapshot
        setTasks(snapshot)
        setToast({ message: result.error })
      }
    },
    [apply]
  )

  const handleAdd = useCallback(
    (title: string) => {
      const id = crypto.randomUUID()
      const positions = tasksRef.current
        .filter((t) => t.bucket === 'today')
        .map((t) => t.position)
      const position = rankBetween(positions.length > 0 ? Math.max(...positions) : null, null)
      const now = new Date().toISOString()
      const optimistic: Task = {
        id,
        title,
        bucket: 'today',
        position,
        bucketedAt: now,
        doneAt: null,
        createdAt: now,
      }
      void mutate(
        (prev) => [...prev, optimistic],
        () => addTask({ id, title, position })
      )
    },
    [mutate]
  )

  const handleToggle = useCallback(
    (id: string, done: boolean) => {
      // bucket 과 position 은 건드리지 않는다.
      // 그래야 완료를 취소했을 때 별도 기록 없이 원래 자리로 돌아온다.
      const doneAt = done ? new Date().toISOString() : null
      void mutate(
        (prev) => prev.map((t) => (t.id === id ? { ...t, doneAt } : t)),
        () => setDone({ id, done })
      )
    },
    [mutate]
  )

  const handleRename = useCallback(
    (id: string, title: string) => {
      void mutate(
        (prev) => prev.map((t) => (t.id === id ? { ...t, title } : t)),
        () => renameTask({ id, title })
      )
    },
    [mutate]
  )

  const handleDelete = useCallback(
    (id: string) => {
      const removed = tasksRef.current.find((t) => t.id === id)
      if (!removed) return

      void mutate(
        (prev) => prev.filter((t) => t.id !== id),
        () => deleteTask({ id })
      )

      // 확인 창은 마찰이고, 되돌리기는 마찰이 아니다.
      setToast({
        message: '삭제했습니다',
        actionLabel: '실행 취소',
        onAction: () => {
          setToast(null)
          void mutate(
            (prev) => [...prev, removed],
            () => restoreTask({ task: removed })
          )
        },
      })
    },
    [mutate]
  )

  const now = new Date()
  const completed = tasks
    .filter((t) => t.doneAt !== null)
    .sort((a, b) => (a.doneAt! < b.doneAt! ? 1 : -1))

  return (
    <div className="mx-auto flex max-w-xl flex-col gap-6 p-4">
      <QuickAdd onSubmit={handleAdd} />

      {BUCKETS.map((bucket) => {
        const items = tasks
          .filter((t) => t.bucket === bucket && t.doneAt === null)
          .sort((a, b) => a.position - b.position)

        return (
          <section key={bucket} className="flex flex-col gap-1">
            <h2 className="flex justify-between text-sm text-neutral-500">
              <span>{BUCKET_LABELS[bucket]}</span>
              <span>{items.length}</span>
            </h2>
            <ul>
              {items.map((task) => (
                <TaskItem
                  key={task.id}
                  task={task}
                  stale={isStale(task, now)}
                  onToggle={(done) => handleToggle(task.id, done)}
                  onRename={(title) => handleRename(task.id, title)}
                  onDelete={() => handleDelete(task.id)}
                />
              ))}
            </ul>
          </section>
        )
      })}

      <CompletedSection tasks={completed} onToggle={handleToggle} />

      {toast ? (
        <Toast
          message={toast.message}
          actionLabel={toast.actionLabel}
          onAction={toast.onAction}
          onDismiss={dismissToast}
        />
      ) : null}
    </div>
  )
}
```

- [ ] **Step 5: 수동 확인**

Run: `npm run dev`

1. 항목을 체크 → 목록에서 사라지고 하단 "완료 (오늘 1개)"에 반영된다
2. 완료 영역을 펼쳐 체크를 해제 → **원래 그룹의 원래 자리로** 돌아온다
3. 제목을 클릭 → 편집 모드. 고치고 엔터 → 반영된다. Esc → 원래 제목으로 되돌아온다
4. × 버튼 → 사라지고 토스트에 "실행 취소"가 뜬다. 누르면 되살아난다
5. 5초 기다리면 토스트가 사라진다
6. 새로고침 → 위의 결과가 모두 유지된다

- [ ] **Step 6: 커밋**

```powershell
git add -A
git commit -m "feat: add complete, edit, delete with undo"
```

---

### Task 10: 드래그로 순서와 그룹 바꾸기

**Files:**
- Modify: `src/components/TaskBoard.tsx`
- Create: `src/components/TaskGroup.tsx`
- Modify: `src/components/TaskItem.tsx` (드래그 핸들)

**Interfaces:**
- Consumes: `rankBetween`, `needsRebalance`, `moveTask`
- Produces: `<TaskGroup bucket={Bucket} tasks={Task[]} ... />`

**설치:**

```powershell
npm install @dnd-kit/core@^6 @dnd-kit/sortable@^10 @dnd-kit/utilities@^3
```

- [ ] **Step 1: 드롭 위치 계산 함수에 테스트 추가**

새 순위는 "어느 그룹의, 몇 번째 자리에 놓였는가"만 알면 정해진다. 이 계산을 `TaskBoard`의 이벤트 핸들러 안에 묻어 두면 테스트할 수 없다. 순수 함수로 꺼낸다.

`src/lib/ranking.test.ts` 끝에 추가:

```typescript
import { positionForDrop } from './ranking'

describe('positionForDrop', () => {
  const group = [
    { id: 'a', position: 0 },
    { id: 'b', position: 1 },
    { id: 'c', position: 2 },
  ]

  it('맨 앞에 놓으면 첫 항목보다 작다', () => {
    expect(positionForDrop(group, 0)).toBe(-1)
  })

  it('중간에 놓으면 위아래 중간값이다', () => {
    expect(positionForDrop(group, 1)).toBe(0.5)
  })

  it('맨 뒤에 놓으면 마지막 항목보다 크다', () => {
    expect(positionForDrop(group, 3)).toBe(3)
  })

  it('빈 그룹에 놓으면 0이다', () => {
    expect(positionForDrop([], 0)).toBe(0)
  })
})
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `npm test`
Expected: FAIL — `positionForDrop is not a function`

- [ ] **Step 3: positionForDrop 구현**

`src/lib/ranking.ts` 끝에 추가:

```typescript
/**
 * 이미 position 오름차순으로 정렬된 그룹의 `index` 번째 자리에 놓을 때의 순위.
 * 옮겨 온 항목 자신은 `group`에서 빠져 있어야 한다.
 */
export function positionForDrop(
  group: readonly { position: number }[],
  index: number
): number {
  const before = index > 0 ? group[index - 1].position : null
  const after = index < group.length ? group[index].position : null
  return rankBetween(before, after)
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test`
Expected: PASS

- [ ] **Step 5: TaskItem을 정렬 가능하게 만들기**

`src/components/TaskItem.tsx`의 `import` 목록에 추가:

```tsx
import { useSortable } from '@dnd-kit/sortable'
import { CSS } from '@dnd-kit/utilities'
```

컴포넌트 본문 맨 위(`const [editing, ...]` 위)에 추가:

```tsx
const { attributes, listeners, setNodeRef, transform, transition, isDragging } =
  useSortable({ id: task.id })
```

`<li ...>`를 다음으로 교체한다:

```tsx
<li
  ref={setNodeRef}
  style={{ transform: CSS.Transform.toString(transform), transition }}
  className={`group flex items-center gap-2 py-1 ${isDragging ? 'opacity-50' : ''}`}
>
  <span
    {...attributes}
    {...listeners}
    aria-label={`${task.title} 순서 바꾸기`}
    className="cursor-grab text-neutral-300"
  >
    ⠿
  </span>
```

(기존의 체크박스부터 삭제 버튼까지는 그대로 두고, 닫는 `</li>`도 그대로 둔다.)

- [ ] **Step 6: TaskGroup 작성**

`src/components/TaskGroup.tsx`:

```tsx
'use client'

import { useDroppable } from '@dnd-kit/core'
import { SortableContext, verticalListSortingStrategy } from '@dnd-kit/sortable'
import { isStale } from '@/lib/staleness'
import { BUCKET_LABELS, type Bucket, type Task } from '@/lib/tasks/types'
import { TaskItem } from './TaskItem'

export function TaskGroup({
  bucket,
  tasks,
  now,
  onToggle,
  onRename,
  onDelete,
}: {
  bucket: Bucket
  tasks: Task[]
  now: Date
  onToggle: (id: string, done: boolean) => void
  onRename: (id: string, title: string) => void
  onDelete: (id: string) => void
}) {
  // 그룹이 비어 있어도 끌어다 놓을 수 있어야 한다.
  const { setNodeRef } = useDroppable({ id: `bucket:${bucket}` })

  return (
    <section className="flex flex-col gap-1">
      <h2 className="flex justify-between text-sm text-neutral-500">
        <span>{BUCKET_LABELS[bucket]}</span>
        <span>{tasks.length}</span>
      </h2>
      <SortableContext
        items={tasks.map((t) => t.id)}
        strategy={verticalListSortingStrategy}
      >
        <ul ref={setNodeRef} className="min-h-8">
          {tasks.map((task) => (
            <TaskItem
              key={task.id}
              task={task}
              stale={isStale(task, now)}
              onToggle={(done) => onToggle(task.id, done)}
              onRename={(title) => onRename(task.id, title)}
              onDelete={() => onDelete(task.id)}
            />
          ))}
        </ul>
      </SortableContext>
    </section>
  )
}
```

- [ ] **Step 7: TaskBoard에 드래그 연결**

`src/components/TaskBoard.tsx`의 import에 추가:

```tsx
import { DndContext, PointerSensor, type DragEndEvent, useSensor, useSensors } from '@dnd-kit/core'
import { needsRebalance, positionForDrop, rebalancedPositions } from '@/lib/ranking'
import { moveTask, rebalanceGroup } from '@/lib/tasks/actions'
import { TaskGroup } from './TaskGroup'
```

컴포넌트 안, `return` 위에 추가:

```tsx
// 클릭(제목 편집)과 드래그가 서로를 잡아먹지 않도록 8px 이동 후에 드래그로 친다.
const sensors = useSensors(useSensor(PointerSensor, { activationConstraint: { distance: 8 } }))

const handleDragEnd = useCallback(
  (event: DragEndEvent) => {
    const { active, over } = event
    if (!over) return

    // 집었다가 제자리에 놓으면 dnd-kit 은 over.id 로 자기 자신을 준다.
    // 이때 아래의 findIndex 는 -1 을 돌려주고 index 가 0 으로 뭉개져서,
    // 가만히 두려던 항목이 그룹 맨 위로 튀어 오른다. 여기서 끊는다.
    if (active.id === over.id) return

    const moved = tasksRef.current.find((t) => t.id === active.id)
    if (!moved) return

    const overId = String(over.id)
    const targetBucket: Bucket = overId.startsWith('bucket:')
      ? (overId.slice('bucket:'.length) as Bucket)
      : (tasksRef.current.find((t) => t.id === overId)?.bucket ?? moved.bucket)

    // 자기 자신을 뺀 대상 그룹을 순서대로 늘어놓고, 몇 번째 자리인지 찾는다.
    const group = tasksRef.current
      .filter((t) => t.bucket === targetBucket && t.doneAt === null && t.id !== moved.id)
      .sort((a, b) => a.position - b.position)

    const overIndex = overId.startsWith('bucket:')
      ? -1
      : group.findIndex((t) => t.id === overId)

    // 대상이 그룹에 없으면 어디에 놓으라는 건지 알 수 없다. 0 번째로 뭉개면
    // 항목이 엉뚱하게 맨 위로 간다. 차라리 아무 것도 하지 않는다.
    if (!overId.startsWith('bucket:') && overIndex === -1) return

    // 같은 그룹 안에서 아래로 끌 때, dnd-kit 의 미리보기는 대상 항목이 위로 밀리고
    // 그 자리를 내가 차지하는 모습을 보여준다. 그런데 group 에서 나 자신을 이미
    // 빼놓았기 때문에, 대상의 "앞"에 넣으면 미리보기보다 한 칸 위에 떨어진다.
    // 아래로 가는 경우에만 대상의 뒤로 보낸다. 위로 끄는 경우는 앞이 맞다.
    const movingDown =
      targetBucket === moved.bucket &&
      overIndex >= 0 &&
      moved.position < group[overIndex].position

    const index = overId.startsWith('bucket:')
      ? group.length
      : movingDown
        ? overIndex + 1
        : overIndex

    // 같은 두 항목 사이에 반복해서 끼워 넣으면 간격이 부동소수점 한계에 닿는다.
    // 그 자리에 더 넣기 전에 그룹 전체를 1 간격으로 펴고, 편 값 위에서 다시 계산한다.
    let layout = group
    const before = index > 0 ? group[index - 1].position : null
    const after = index < group.length ? group[index].position : null

    if (needsRebalance(before, after)) {
      const evened = rebalancedPositions(group.length)
      const positions = group.map((t, i) => ({ id: t.id, position: evened[i] }))
      const byId = new Map(positions.map((p) => [p.id, p.position]))

      // 상태를 직접 고치지 않고 편 값을 담은 새 배열을 만든다.
      layout = group.map((t, i) => ({ ...t, position: evened[i] }))

      void mutate(
        (prev) => prev.map((t) => (byId.has(t.id) ? { ...t, position: byId.get(t.id)! } : t)),
        () => rebalanceGroup({ positions })
      )
    }

    const position = positionForDrop(layout, index)
    const bucketChanged = targetBucket !== moved.bucket
    const bucketedAt = bucketChanged ? new Date().toISOString() : moved.bucketedAt

    void mutate(
      (prev) =>
        prev.map((t) =>
          t.id === moved.id ? { ...t, bucket: targetBucket, position, bucketedAt } : t
        ),
      () => moveTask({ id: moved.id, bucket: targetBucket, position })
    )
  },
  [mutate]
)
```

`BUCKETS.map(...)` 블록 전체를 다음으로 교체한다:

```tsx
<DndContext sensors={sensors} onDragEnd={handleDragEnd}>
  {BUCKETS.map((bucket) => (
    <TaskGroup
      key={bucket}
      bucket={bucket}
      now={now}
      tasks={tasks
        .filter((t) => t.bucket === bucket && t.doneAt === null)
        .sort((a, b) => a.position - b.position)}
      onToggle={handleToggle}
      onRename={handleRename}
      onDelete={handleDelete}
    />
  ))}
</DndContext>
```

`Bucket` 타입을 import 목록에 추가한다.

- [ ] **Step 8: 타입 검사와 수동 확인**

Run: `npx tsc --noEmit`
Expected: 오류 없음

Run: `npm run dev`

1. ⠿ 핸들을 잡고 같은 그룹 안에서 위아래로 옮긴다 → 순서가 바뀌고 새로고침해도 유지된다
2. "오늘"의 항목을 "언젠가"로 끌어다 놓는다 → 그룹이 바뀌고 개수가 갱신된다
3. 빈 그룹에도 끌어다 놓을 수 있다
4. 제목을 **클릭**하면 여전히 편집 모드로 들어간다 (드래그와 충돌하지 않는다)

- [ ] **Step 9: 커밋**

```powershell
git add -A
git commit -m "feat: add drag and drop reordering across buckets"
```

---

### Task 11: 사이드바 — 요약과 검색

**Files:**
- Create: `src/components/Sidebar.tsx`
- Modify: `src/components/TaskBoard.tsx`

**Interfaces:**
- Consumes: `Task`, `BUCKET_LABELS`
- Produces: `<Sidebar counts={Record<Bucket, number>} doneToday={number} query={string} onQueryChange includeDone onIncludeDoneChange />`

사이드바는 스스로 데이터를 조회하지 않는다. 집계값을 props로 받는다. 검색도 서버를 부르지 않고 이미 메모리에 있는 목록을 거른다 — 혼자 쓰는 앱의 항목 수는 클라이언트에서 거르기에 충분히 적고, 그 편이 즉각적이다. (`searchTasks`는 목록이 감당 못 할 만큼 커졌을 때를 위해 남겨 둔다.)

- [ ] **Step 1: Sidebar 작성**

`src/components/Sidebar.tsx`:

```tsx
'use client'

import { BUCKETS, BUCKET_LABELS, type Bucket } from '@/lib/tasks/types'

export function Sidebar({
  counts,
  doneToday,
  query,
  onQueryChange,
  includeDone,
  onIncludeDoneChange,
}: {
  counts: Record<Bucket, number>
  doneToday: number
  query: string
  onQueryChange: (value: string) => void
  includeDone: boolean
  onIncludeDoneChange: (value: boolean) => void
}) {
  return (
    <aside className="hidden w-[220px] shrink-0 flex-col gap-6 p-4 lg:flex">
      <div className="flex flex-col gap-1 text-sm">
        {BUCKETS.map((bucket) => (
          <div key={bucket} className="flex justify-between text-neutral-500">
            <span>{BUCKET_LABELS[bucket]}</span>
            <span>{counts[bucket]}</span>
          </div>
        ))}
        <div className="mt-2 flex justify-between text-neutral-400">
          <span>오늘 완료</span>
          <span>{doneToday}</span>
        </div>
      </div>

      <div className="flex flex-col gap-2">
        <input
          value={query}
          onChange={(e) => onQueryChange(e.target.value)}
          onKeyDown={(e) => {
            if (e.key === 'Escape') onQueryChange('')
          }}
          placeholder="검색"
          aria-label="할 일 검색"
          className="rounded border px-2 py-1 text-sm outline-none focus:border-neutral-900"
        />
        <label className="flex items-center gap-2 text-sm text-neutral-500">
          <input
            type="checkbox"
            checked={includeDone}
            onChange={(e) => onIncludeDoneChange(e.target.checked)}
          />
          완료 포함
        </label>
      </div>
    </aside>
  )
}
```

- [ ] **Step 2: TaskBoard에 사이드바와 검색 모드 연결**

`src/components/TaskBoard.tsx`의 import에 `Sidebar`를 추가한다.

**추가 위치를 두 군데로 나눈다.** 아래 블록은 `now`를 쓰는데 `now`는 `const completed = ...` 바로 위에서 선언되므로, 통째로 위쪽에 붙이면 `Cannot access 'now' before initialization`으로 죽는다.

먼저 두 상태만 다른 `useState` 선언들 옆, 컴포넌트 맨 위에 둔다:

```tsx
const [query, setQuery] = useState('')
const [includeDone, setIncludeDone] = useState(false)
```

나머지 파생값은 `const now = new Date()` **아래**, `return` 위에 둔다:

```tsx
const counts = {
  today: tasks.filter((t) => t.bucket === 'today' && t.doneAt === null).length,
  week: tasks.filter((t) => t.bucket === 'week' && t.doneAt === null).length,
  someday: tasks.filter((t) => t.bucket === 'someday' && t.doneAt === null).length,
}

// now 는 위에서 이미 선언돼 있다. 여기서 다시 만들지 않는다 —
// 한 번의 렌더 안에서 두 시각이 갈리면 "오래됨" 판정과 "오늘 완료" 집계가
// 서로 다른 순간을 기준으로 삼게 된다.
const doneToday = tasks.filter(
  (t) => t.doneAt !== null && new Date(t.doneAt).toDateString() === now.toDateString()
).length

const searching = query.trim().length > 0
const searchResults = searching
  ? tasks
      .filter((t) => t.title.toLowerCase().includes(query.trim().toLowerCase()))
      .filter((t) => includeDone || t.doneAt === null)
      .sort((a, b) => a.position - b.position)
  : []
```

`return` 문 전체를 다음 구조로 바꾼다:

```tsx
return (
  <div className="mx-auto flex max-w-5xl">
    <Sidebar
      counts={counts}
      doneToday={doneToday}
      query={query}
      onQueryChange={setQuery}
      includeDone={includeDone}
      onIncludeDoneChange={setIncludeDone}
    />

    <div className="flex min-w-0 flex-1 flex-col gap-6 p-4">
      {/* 좁은 화면에서는 사이드바 대신 헤더 한 줄로 요약한다. */}
      <p className="text-sm text-neutral-400 lg:hidden">
        오늘 {counts.today} · 이번주 {counts.week} · 언젠가 {counts.someday}
      </p>

      <QuickAdd onSubmit={handleAdd} />

      {searching ? (
        // 검색 결과에서는 드래그를 끈다. 순서가 그룹 기준이 아니라 놓을 자리가 정의되지 않는다.
        <ul>
          {searchResults.map((task) => (
            <TaskItem
              key={task.id}
              task={task}
              stale={false}
              onToggle={(done) => handleToggle(task.id, done)}
              onRename={(title) => handleRename(task.id, title)}
              onDelete={() => handleDelete(task.id)}
            />
          ))}
          {searchResults.length === 0 ? (
            <li className="py-2 text-sm text-neutral-400">결과가 없습니다</li>
          ) : null}
        </ul>
      ) : (
        <>
          <DndContext sensors={sensors} onDragEnd={handleDragEnd}>
            {BUCKETS.map((bucket) => (
              <TaskGroup
                key={bucket}
                bucket={bucket}
                now={now}
                tasks={tasks
                  .filter((t) => t.bucket === bucket && t.doneAt === null)
                  .sort((a, b) => a.position - b.position)}
                onToggle={handleToggle}
                onRename={handleRename}
                onDelete={handleDelete}
              />
            ))}
          </DndContext>

          <CompletedSection tasks={completed} onToggle={handleToggle} />
        </>
      )}

      {toast ? (
        <Toast
          message={toast.message}
          actionLabel={toast.actionLabel}
          onAction={toast.onAction}
          onDismiss={dismissToast}
        />
      ) : null}
    </div>
  </div>
)
```

검색 결과 안에서 `TaskItem`은 `useSortable`을 쓰지만 `SortableContext` 밖에 있다. dnd-kit은 이 경우 드래그가 비활성화된 상태로 정상 동작한다.

- [ ] **Step 3: 타입 검사와 수동 확인**

Run: `npx tsc --noEmit`
Expected: 오류 없음

Run: `npm run dev`

1. 넓은 창(1024px 이상) → 왼쪽에 개수 요약과 검색창이 보인다
2. 창을 좁힌다 → 사이드바가 사라지고 상단에 `오늘 n · 이번주 n · 언젠가 n` 한 줄이 나타난다
3. 검색어를 입력 → 목록이 결과로 바뀐다. 완료 포함을 켜면 완료 항목도 나온다
4. 검색창을 비우거나 Esc → 원래 목록으로 돌아온다

- [ ] **Step 4: 커밋**

```powershell
git add -A
git commit -m "feat: add sidebar with summary and search"
```

---

### Task 12: PWA

홈 화면 아이콘으로 앱처럼 열기 위한 것이지 오프라인 동작을 위한 것이 아니다. 그래도 서비스 워커는 하나 둔다 — Android Chrome이 설치를 허용하려면 fetch 핸들러를 가진 서비스 워커가 필요하기 때문이다. 캐싱 전략은 넣지 않는다.

**Files:**
- Create: `public/manifest.webmanifest`
- Create: `public/sw.js`
- Create: `public/icon-192.png`, `public/icon-512.png`
- Create: `src/components/ServiceWorkerRegistrar.tsx`
- Modify: `src/app/layout.tsx`

**Interfaces:**
- Consumes: 없음
- Produces: `<ServiceWorkerRegistrar />`

- [ ] **Step 1: manifest 작성**

`public/manifest.webmanifest`:

```json
{
  "name": "순서",
  "short_name": "순서",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#171717",
  "icons": [
    { "src": "/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/icon-512.png", "sizes": "512x512", "type": "image/png" }
  ]
}
```

- [ ] **Step 2: 아이콘 만들기**

192×192와 512×512 PNG를 `public/`에 둔다. 어두운 배경(`#171717`)에 흰 글자 "순"이면 충분하다. 이미지 편집 도구가 없으면 다음으로 만든다:

```powershell
Add-Type -AssemblyName System.Drawing
foreach ($size in 192, 512) {
  $bmp = New-Object System.Drawing.Bitmap($size, $size)
  $g = [System.Drawing.Graphics]::FromImage($bmp)
  $g.Clear([System.Drawing.ColorTranslator]::FromHtml("#171717"))
  $font = New-Object System.Drawing.Font("Malgun Gothic", ($size * 0.5), [System.Drawing.FontStyle]::Bold)
  $brush = [System.Drawing.Brushes]::White
  $fmt = New-Object System.Drawing.StringFormat
  $fmt.Alignment = [System.Drawing.StringAlignment]::Center
  $fmt.LineAlignment = [System.Drawing.StringAlignment]::Center
  $rect = New-Object System.Drawing.RectangleF(0, 0, $size, $size)
  $g.DrawString("순", $font, $brush, $rect, $fmt)
  $g.Dispose()
  $bmp.Save("$PWD\public\icon-$size.png", [System.Drawing.Imaging.ImageFormat]::Png)
  $bmp.Dispose()
}
```

- [ ] **Step 3: 서비스 워커 작성**

`public/sw.js`:

```javascript
// 설치 가능 조건을 만족시키기 위한 최소 서비스 워커.
// 이 앱은 오프라인 동작을 목표로 하지 않으므로 캐시 전략은 없다.
self.addEventListener('install', () => self.skipWaiting())
self.addEventListener('activate', (event) => event.waitUntil(self.clients.claim()))

// fetch 핸들러는 "존재"하기만 하면 설치 조건을 만족한다. respondWith 를 부르지 않으면
// 브라우저가 원래대로 네트워크에 나간다.
//
// event.respondWith(fetch(event.request)) 로 되받아 넘기고 싶은 유혹이 있지만 그러면
// 모든 요청이 서비스 워커를 한 번 거쳐 가게 되고, navigation preload 가 꺼지고,
// range 요청이나 스트리밍 응답에서 미묘하게 어긋날 수 있다. 아무 이득도 없이
// 실패 지점만 하나 늘리는 셈이다.
self.addEventListener('fetch', () => {})
```

- [ ] **Step 4: 등록 컴포넌트 작성**

`src/components/ServiceWorkerRegistrar.tsx`:

```tsx
'use client'

import { useEffect } from 'react'

export function ServiceWorkerRegistrar() {
  useEffect(() => {
    if (process.env.NODE_ENV !== 'production') return
    if (!('serviceWorker' in navigator)) return
    navigator.serviceWorker.register('/sw.js').catch(() => {
      // 등록 실패는 앱 동작에 영향이 없다. 홈 화면 설치만 안 될 뿐이다.
    })
  }, [])

  return null
}
```

- [ ] **Step 5: layout에 연결**

`src/app/layout.tsx`의 `metadata` 객체에 다음을 추가한다:

```tsx
manifest: '/manifest.webmanifest',
```

그리고 `<body>` 안에 `{children}` 옆으로 `<ServiceWorkerRegistrar />`를 넣고, 파일 상단에 import를 추가한다.

- [ ] **Step 6: 프로덕션 빌드로 확인**

```powershell
npm run build
npm start
```

브라우저에서 `http://localhost:3000` 접속 후 DevTools > Application:
- Manifest에 "순서"와 아이콘 두 개가 보인다
- Service Workers에 `sw.js`가 activated 상태다

확인 후 Ctrl+C.

- [ ] **Step 7: 커밋**

```powershell
git add -A
git commit -m "feat: add PWA manifest and service worker"
```

---

### Task 13: E2E 테스트

회귀를 잡는 최소한의 그물 하나. 로그인부터 완료까지 한 흐름을 훑는다.

**Files:**
- Create: `playwright.config.ts`
- Create: `e2e/task-flow.spec.ts`
- Modify: `package.json` (`test:e2e` 스크립트)
- Modify: `.gitignore`

**Interfaces:**
- Consumes: Task 4에서 만든 테스트 계정 (`test@example.com` / `test-password-1234`), 원격 Supabase 프로젝트
- Produces: `npm run test:e2e`

E2E도 실제 프로젝트에 쓴다. 그래서 두 가지를 지킨다. 항목 제목에 타임스탬프를 붙여 매 실행마다 다르게 만들고 — 반복 실행이 서로를 밟지 않게 —, `afterEach`에서 그 항목을 지운다. 정리를 사람 손에 맡기면 실행할 때마다 실제 목록에 찌꺼기가 하나씩 쌓인다.

정리는 제목으로 **찾되** id 로만 **지운다**. 제목 패턴으로 삭제하는 코드는 만들지 않는다 — 통합 테스트에 적용한 규칙과 같다.

- [ ] **Step 1: Playwright 설치**

```powershell
npm install --save-dev @playwright/test@^1
npx playwright install chromium
```

- [ ] **Step 2: 설정 파일 작성**

`playwright.config.ts`:

```typescript
import { defineConfig } from '@playwright/test'

export default defineConfig({
  testDir: './e2e',
  use: { baseURL: 'http://localhost:3000' },
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:3000',
    reuseExistingServer: true,
    timeout: 120_000,
  },
})
```

- [ ] **Step 3: 스크립트와 gitignore 추가**

`package.json`의 `scripts`에 추가:

```json
"test:e2e": "playwright test"
```

`.gitignore`에 추가:

```
/test-results
/playwright-report
```

- [ ] **Step 4: E2E 테스트 작성**

`e2e/task-flow.spec.ts`:

```typescript
import { createClient } from '@supabase/supabase-js'
import { config } from 'dotenv'
import { expect, test } from '@playwright/test'

config({ path: '.env.test' })

const EMAIL = 'test@example.com'
const PASSWORD = 'test-password-1234'

/**
 * 이 테스트는 실제 프로젝트에 행을 만든다. 정리하지 않으면 실행할 때마다
 * 사용자의 목록에 찌꺼기가 하나씩 쌓인다.
 * 제목으로 찾되 삭제는 id 로만 한다 — 제목 패턴으로 지우는 삭제는 만들지 않는다.
 */
async function removeTaskByTitle(title: string) {
  const supabase = createClient(
    process.env.SUPABASE_TEST_URL!,
    process.env.SUPABASE_TEST_ANON_KEY!
  )
  await supabase.auth.signInWithPassword({ email: EMAIL, password: PASSWORD })

  const { data } = await supabase.from('tasks').select('id').eq('title', title)
  if (data && data.length > 0) {
    await supabase.from('tasks').delete().in('id', data.map((row) => row.id))
  }
}

// 같은 계정으로 반복 실행되므로 제목을 매번 다르게 만든다.
const title = `E2E 항목 ${Date.now()}`

// 단언이 중간에 실패해도 정리는 돌아야 한다. try/finally 대신 afterEach 를 쓰는 이유는,
// 실패 시점이 어디든 훅은 실행되기 때문이다.
test.afterEach(async () => {
  await removeTaskByTitle(title)
})

test('로그인하고 할 일을 추가한 뒤 완료한다', async ({ page }) => {
  await page.goto('/')
  await expect(page).toHaveURL(/\/login/)

  await page.getByPlaceholder('이메일').fill(EMAIL)
  await page.getByPlaceholder('비밀번호').fill(PASSWORD)
  await page.getByRole('button', { name: '로그인' }).click()

  const input = page.getByLabel('할 일 입력')
  await expect(input).toBeVisible()

  await input.fill(title)
  await input.press('Enter')

  // 낙관적 업데이트라 즉시 보여야 한다.
  await expect(page.getByText(title)).toBeVisible()

  // 새로고침해도 남아 있어야 실제로 저장된 것이다.
  await page.reload()
  await expect(page.getByText(title)).toBeVisible()

  await page.getByLabel(`${title} 완료`).check()

  // 완료하면 목록에서 빠지고 완료 영역으로 간다.
  await expect(page.getByText(title)).toBeHidden()
  await page.getByRole('button', { name: /완료 \(오늘/ }).click()
  await expect(page.getByText(title)).toBeVisible()
})
```

이 테스트는 브라우저를 실제로 몰기 때문에, Task 5 리뷰에서 미뤄둔 항목 하나를 여기서 닫는다:
로그인 후 `page.reload()` 가 통과한다는 것은 세션 쿠키가 `Secure` / `SameSite` / `HttpOnly` 속성을
달고도 실제 브라우저에서 살아남는다는 뜻이다. curl 로는 확인할 수 없던 부분이다.

- [ ] **Step 5: 테스트 실행**

Run: `npm run test:e2e`
Expected: `1 passed`

- [ ] **Step 6: 전체 검사**

```powershell
npm test
npm run test:integration
npx tsc --noEmit
npm run lint
npm run build
```

Expected: 다섯 개 모두 성공

- [ ] **Step 7: 커밋**

```powershell
git add -A
git commit -m "test: add end-to-end task flow"
```

---

### Task 14: 프로덕션 배포

**Files:**
- Create: `README.md`

**Interfaces:**
- Consumes: 완성된 앱
- Produces: 접속 가능한 URL

스키마는 Task 4에서 이미 이 프로젝트에 적용됐다. 개발과 배포가 같은 프로젝트를 쓴다.

- [ ] **Step 1: 실제로 쓸 계정 만들기**

대시보드 Authentication > Users에서 본인 계정을 만든다. `test@example.com`은 테스트용이므로 실사용 계정을 따로 둔다.

- [ ] **Step 2: 가입 막기**

Authentication > Providers > Email에서 **"Enable Sign Ups"를 끈다.** 앱에 가입 폼이 없어도 API로는 가입이 가능하므로, 여기서 막아야 실제로 닫힌다.

주의: 이걸 끄면 `test@example.com`과 `other@example.com`을 새로 만들 수 없다. 이미 만들어져 있는 계정은 계속 쓸 수 있으므로 테스트는 영향받지 않지만, 나중에 테스트 계정을 다시 만들어야 하면 잠시 켜야 한다.

- [ ] **Step 3: 보안 점검**

대시보드 Advisors > Security를 연다.

Expected: `tasks` 테이블에 대한 경고 없음. RLS 미적용이나 정책 누락 경고가 뜨면 해결한 뒤 배포한다.

- [ ] **Step 4: Vercel 배포**

```powershell
npx --yes vercel
```

프로젝트 설정에서 환경변수 두 개를 넣는다 — `.env.local`과 같은 값이다:
- `NEXT_PUBLIC_SUPABASE_URL` = `https://cwxbzvevvoxdarekeldb.supabase.co`
- `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` = `sb_publishable_0NCtH9t7_tbtV27W685pxg_I0h1uwCy`

`service_role` 키는 넣지 않는다.

- [ ] **Step 5: 배포 확인**

배포된 URL에서:
1. 로그인이 된다
2. 할 일을 추가하면 저장된다
3. 휴대폰으로 접속해 홈 화면에 추가하면 앱처럼 열린다
4. PC에서 추가한 항목이 휴대폰에서 보인다

- [ ] **Step 6: README 작성**

`README.md`:

```markdown
# 순서 (sunseo)

개인용 할 일 관리. 한 줄로 던져 넣고, 중요도 3단계로 나뉜 전체 목록을 한 화면에서 본다.

## 개발

원격 Supabase 프로젝트를 쓴다. Docker는 필요 없다.

    cp .env.local.example .env.local   # 프로젝트 URL 과 publishable key 를 채운다
    npm install
    npm run dev

## 테스트

    npm test                  # 단위 (Vitest). 네트워크 불필요
    npm run test:integration  # RLS·읽기 계층 (원격 Supabase 에 실제로 쓴다)
    npm run test:e2e          # E2E (Playwright, 원격 Supabase 사용)

통합 테스트와 E2E 는 실제 프로젝트 데이터베이스에 쓴다. 통합 테스트는 만든 행을 스스로 지우고,
E2E 는 매번 다른 제목을 만들어 서로 밟지 않게 한다.

## 문서

- 설계: `docs/superpowers/specs/2026-07-31-personal-task-manager-design.md`
- 구현 계획: `docs/superpowers/plans/2026-07-31-sunseo.md`
```

- [ ] **Step 7: 커밋**

```powershell
git add -A
git commit -m "docs: add README"
```
